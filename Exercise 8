const firstName =('William'); //add your first name
const lastName =('Powell');//add your last name
const fullNameConcat =('William Powell'); //create your full name from the first two variables using concatenation 
const fullNameInterp =('William Powell'); //create your full name from the first two variables using interpolation

//don't change the following line, checks to see that concatenation and interpolation gave results
console.log("Are your fullNameConcat and fullNameInterp variables equal?")
console.log(fullNameConcat == fullNameInterp); 

let myAge =(24); //add your age
let faveFood =('Pizza'); //add your favorite food
let faveColor =('Red'); //add your favorite color
let miniBio =`My name is ${fullNameInterp} I am ${myAge} and I love ${faveFood} and the color ${faveColor}` //use string interpolation to create summary that includes your full name, age, favorite food, and favorite color
console.log("Your mini bio is:");
console.log(miniBio);